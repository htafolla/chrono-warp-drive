import TransportControl from "@/components/TransportControl";

export default function TPTTPage() {
  return (
    <div className="min-h-screen bg-[#0a0a0f] flex items-center justify-center p-8">
      <div className="w-full max-w-lg">
        <TransportControl />
      </div>
    </div>
  );
}
