[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "tsx ${ENTRY_POINT:-server.ts}"
# No healthcheck: tfjs/transformers boot exceeds Railway windows and rolls back
# listings (well-known JSON). Process bind on PORT is the ready signal.
